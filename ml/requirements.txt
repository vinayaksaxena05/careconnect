# Training + evaluation dependencies for the triage model.
#
# The FastAPI backend only needs the inference subset, which is already listed
# in backend/requirements.txt (scikit-learn, pandas, numpy, xgboost, shap,
# joblib). Install this file only when you intend to (re)train.

scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
xgboost>=2.0
shap>=0.44
joblib>=1.3

# Tests
pytest>=8.0

# Notebooks / exploratory analysis (optional)
matplotlib>=3.8
jupyter>=1.0
